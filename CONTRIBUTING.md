
```
<type>(<scope>)<author>: <short summary>

[optional body]

[optional footer]
```

---

## 🔹 Поля

- **type** — тип изменения (feat, fix, docs, chore, refactor, style, test и т.д.)
    
- **scope** — область, где произошло изменение (например: auth, api, ui).
    
- **author** — подпись/ник человека (обязательно в формате `<nickname>`).
    
- **summary** — краткое описание изменений.
    
- **body** (опционально) — подробное объяснение.
    
- **footer** (опционально) — ссылки на задачи, тикеты или пояснения.
    

---

## 🔹 Допустимые типы коммитов

| Тип                | Для чего используется                                     |
| ------------------ | --------------------------------------------------------- |
| **feat**           | Новый функционал                                          |
| **fix**            | Исправление бага                                          |
| **docs**           | Изменения в документации                                  |
| **style**          | Форматирование (без изменения логики)                     |
| **refactor**       | Рефакторинг без изменения поведения                       |
| **perf**           | Оптимизация производительности                            |
| **test**           | Добавление/изменение тестов                               |
| **chore**          | Вспомогательные изменения (сборка, конфиги, CI/CD)        |
| add                | Добавление каких-нибудь файлов (спрайтов или иных файлов) |
| upd(update)        | обновление (например уровня)                              |
| upd(update)_editor | обновление движка                                         |
| upl(upload)        | загрузка чего-либо в репозиторий (например пустой проект) |

---

## 🔹 Как связывать коммиты с задачами (GitHub Issues)

В **footer** коммита можно указывать номер задачи:

- Чтобы **закрыть задачу автоматически** при мерже:
    
    ```
    Closes #42
    Fixes #42
    Resolves #42
    ```
    
- Чтобы **просто сослаться на задачу** (без закрытия):
    
    ```
    Related to #57
    See #57
    ```
    
- Чтобы связать **несколько задач**:
    
    ```
    Closes #101
    Related to #102
    ```
    

---

## 🔹 Рекомендации

- Использовать **английский язык для type и scope**, русский или английский для summary.
    
- Подпись **всегда** должна быть: `<nickname>`.
    
- Если коммит связан с задачей — обязательно указываем её номер в footer.
    
- Один коммит = одна логическая задача.
    
- Если изменений много → делай **несколько коммитов**.
    

---
